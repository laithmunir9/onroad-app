# OnRoad client

React + Vite frontend for the driver and rider dashboards. See the
[repo root README](../README.md) for how this fits with the server and how
to run both together.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
npm run lint
```
